version: 4.0.70
steps:
  - prompt: Click on the "Try for Free" button.
    commands:
      - command: hover-text
        text: Try for free
        description: button in the header
        action: click
  - prompt: >-
      Assert that the sign-up or free trial form loads.  # Use the content of
      the file from the environment
    commands:
      - command: assert
        expect: the sign-up form is visible
