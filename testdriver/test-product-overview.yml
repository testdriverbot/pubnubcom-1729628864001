version: 4.0.70
steps:
  - prompt: Click on the "Products" link in the header.
    commands:
      - command: hover-text
        text: Products
        description: link in the header
        action: click
  - prompt: >-
      Assert that a product overview or list is shown.  # Use the content of the
      file from the environment
    commands:
      - command: assert
        expect: a product overview or list is shown
